# Lesson 05: Continue to build on grading program

# Functionalize the letter-grade calc so can repeat with average number grade
#   first within same file, and then imported from src

# Use matplotlib to make line plot of number grades and histogram of letter grades
#   first external library (aka "site package"), added to base env with conda with YML specification
# conda env create -f /home/terencebarrett/PycharmProjects/tutoring/environment.yml
# import matplotlib.pyplot as plt
# fig_1 = plt.figure()
# plt.plot(names, number_grades, 'bd', label='Grades')
# plt.plot(names, [round(mean(number_grades))] * len(names), 'r:', label='Class Average')
# plt.xlabel('Student')
# plt.ylabel('Grade')
# plt.legend()
# plt.title(f'Test results for the {len(names)} students (class average: {round(mean(number_grades))})')
# plt.grid()
# plt.savefig('plots/grades.png')
# plt.show()

# HW: Code remaining TODOs listed in the script
